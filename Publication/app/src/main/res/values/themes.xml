<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Publicationapp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Item-item lain dalam tema -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" >@color/hijau_tua</item>
    </style>

    <style name="Theme.Publicationapp.Dark" parent="Theme.MaterialComponents">
        <!-- Item-item lain dalam tema -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/hijau_tua</item>
    </style>

    <style name="Theme.searchView" parent="Widget.AppCompat.SearchView">
        <!-- Item-item lain dalam tema -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
    </style>


    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="itemIconTint">@color/hijau_tua</item>
        <item name="itemTextColor">@color/hijau_tua</item>
    </style>




</resources>